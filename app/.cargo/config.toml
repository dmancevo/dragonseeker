[build]
# Optimize for size in release builds
[profile.release]
opt-level = 3
lto = true

[target.x86_64-unknown-linux-musl]
# For Alpine Linux builds
rustflags = ["-C", "target-feature=+crt-static"]
